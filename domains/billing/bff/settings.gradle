rootProject.name = 'billing-bff'

// Composite build: pull the thin `demo.bff:domain-sdk` (HeaderIdentity,
// AuthClaims, PolicyRuleClient, PolicyRuleGate) from the sibling source tree.
// At repo-split this line is replaced by a registry repository; the dependency
// coordinate stays the same.
includeBuild('../../../domain-sdk')
