parent=keycloak
import=common/keycloak

# stylesCommon intentionally NOT set: the PatternFly base stylesheet KC's
# default theme ships with overrides P1's typography + form metrics, so the
# GeoWealth login looks different from P1. The theme is fully self-contained
# CSS-wise (resources/css/styles.css declares its own resets + @font-faces).
styles=css/styles.css
scripts=js/auth-spa.js

locales=en
