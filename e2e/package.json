{
  "name": "keycloak-demo-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for the cross-subdomain SSO architecture (cross-subdomain-sso-implementation.md). Drives a live docker compose stack — see e2e/README.md.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.3"
  }
}
