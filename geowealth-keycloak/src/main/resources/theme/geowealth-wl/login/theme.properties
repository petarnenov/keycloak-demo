# GeoWealth white-label login theme.
#
# Phase 1: inherits all templates from keycloak.v2 (PatternFly 5) and only
# overrides the CSS variables. Phase 2 will introduce template.ftl with
# dynamic <style>:root { ... }</style> injection driven by the
# ThemeSelectorProvider's brand context.

parent=keycloak.v2
import=common/keycloak

styles=css/geowealth-wl.css
